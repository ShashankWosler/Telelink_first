# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time

# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:

  push:

    branches: [ main ]

  pull_request:

    branches: [ main ]



jobs:

  build:



    runs-on: ubuntu-latest



    steps:

    - uses: actions/checkout@v2

    - name: Set up JDK 11

      uses: actions/setup-java@v2
      with:


        java-version: '17'

        distribution: 'adopt'

        cache: maven

    - name: Build with Maven

      run: mvn -B package --file pom.xml

      email_config:
  sender_email: shashank.tiwari@wosler.ca
  receiver_email: shashank.tiwari@wosler.ca
  smtp_server: smtp.wosler.ca
  smtp_port: 465  # Use 587 if you're not using SSL/TLS
  smtp_username: your_email@wosler.ca
  smtp_password: your_smtp_password  # Replace with your actual SMTP password

attachment:
  path: /path/to/your/report.pdf
  filename: report.pdf

message:
  subject: "Report Attachment"
  body: |
    Hi Shashank,

    Please find the attached report.

    Best regards,
    Your Name
